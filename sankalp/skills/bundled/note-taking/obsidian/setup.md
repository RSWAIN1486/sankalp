# Obsidian Setup

Configure an Obsidian vault path in Sankalp settings. Sankalp stores durable memory as
Markdown and can open notes directly in Obsidian when the app is installed.
When you use `/remember`, Sankalp now tries to match existing note/folder structure first and
only falls back to `Inbox` if no relevant destination can be inferred.
